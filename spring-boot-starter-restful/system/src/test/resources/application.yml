spring:
  profiles:
    active: dev
  data:
    mongodb:
      database: backend
      username: backend
      password: backend
      authentication-database: admin
logging:
  level:
    root: warn
    com:
      voc: info
api:
  system:
    prefix: system
